<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EJS</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://cdn.tailwindcss.com/"></script>
</head>
<body>
  <div class="w-full h-full bg-zinc-900 p-10 text-white min-h-screen">

    <div class="nav mb-10">
      <a class="text-blue-500" href="/">Home Page</a>
    </div>
    
    <h1 class="text-3xl tracking-tighter mb-5 text-center">All Users</h1>

    <div class="users flex gap-3 flex-wrap justify-center align-center">


      <% if(allUsers.length > 0) {  %>
        <% allUsers.forEach((user) => {  %>
          <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
            <div class="w-full h-52 bg-zinc-500 rounded-lg">
              <img class="w-full h-full" src="<%= user.image %>" alt="Image">
            </div>
            <h3 class="text-xl tracking-tighter mt-4"><%= user.name %></h3>
            <h5 class="text-zinc-bg500"><%= user.email %></h5>
            <div class="flex gap-3 mt-8">
              <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
              <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
            </div>
          </div>
        <% });  %>
      <% } else {  %>
        <h2>No User yet.</h2>
      <% }  %>

      <!-- <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
        <div class="w-full h-52 bg-zinc-500 rounded-lg"></div>
        <h3 class="text-xl tracking-tighter mt-4">Shamim Pervez</h3>
        <h5 class="text-zinc-bg500">shamim@gmail.com</h5>
        <div class="flex gap-3 mt-8">
          <a class="text-zinc-200" href="#">Edit User</a>
          <a class="text-red-500" href="#">Delete User</a>
        </div>
      </div> -->


    </div>
    
  </div>
</body>
</html>